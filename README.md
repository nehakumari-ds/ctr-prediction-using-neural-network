# ctr-prediction-using-neural-network
This project uses a neural network built with TensorFlow and Keras to predict Click-Through Rate (CTR) based on Google Search Console–style content analytics data. It includes data preprocessing, feature scaling with StandardScaler, model training, validation, and performance evaluation.
